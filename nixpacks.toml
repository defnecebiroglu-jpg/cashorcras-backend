[phases.setup]
nixPkgs = ['nodejs_18']
aptPkgs = ['python3', 'build-essential']

[phases.install]
cmds = ['npm ci --include=dev']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node dist/index.js'